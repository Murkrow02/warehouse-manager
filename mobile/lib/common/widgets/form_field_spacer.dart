import 'package:flutter/material.dart';

class FormFieldSpacer extends StatelessWidget {
  const FormFieldSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 16);
  }
}
